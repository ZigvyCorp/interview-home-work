import React from 'react';
import { Link } from 'react-router-dom';

const HeaderLogo = () => {
    return (
        <Link to="/">
            Zigvy Blog
        </Link>
    );
};

export default HeaderLogo;