{
	"name": "express",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test:user": "npm run db:seed && node node_modules/.bin/jest --verbose --runInBand --forceExit --detectOpenHandles --testPathPattern=tests/user.test.js",
		"test:post": "npm run db:seed && node node_modules/.bin/jest --verbose --runInBand --forceExit --detectOpenHandles --testPathPattern=tests/post.test.js",
		"test:comment": "npm run db:seed && node node_modules/.bin/jest --verbose --runInBand --forceExit --detectOpenHandles --testPathPattern=tests/comment.test.js",
		"dev": "nodemon server.js && npm run db:seed",
		"start": "node server.js",
		"db:seed": "node db/seed.js"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.4.5",
		"express": "^4.18.2",
		"express-validator": "^7.0.1",
		"http": "^0.0.1-security",
		"mongoose": "^8.2.0",
		"morgan": "^1.10.0",
		"nodemon": "^3.1.0"
	},
	"devDependencies": {
		"cross-env": "^7.0.3",
		"jest": "^29.7.0",
		"supertest": "^6.3.4"
	}
}
