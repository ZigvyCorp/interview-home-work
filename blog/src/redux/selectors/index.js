export const postsState$ = (state) => state.posts.listpost;

export const modalState$ = (state) => state.modal;

export const commentsState$ = (state) => state.comments.data;

export const userState$ = (state) => state.user.isLoggined;

export const userCurrentState$ = (state) => state.user.user;
