import React from "react";
import { Styled } from "./sidebar.styled";
interface IProps {}

const Sidebar = (props: IProps) => {
  return <Styled className="sidebar"></Styled>;
};

export default Sidebar;
